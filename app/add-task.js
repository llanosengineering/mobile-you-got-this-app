import { Text, View } from "react-native";

const AddTask = () => {
  return (
    <View>
      <Text>Add a task</Text>
    </View>

    // Input form
    // User should be able to add multiple at once
    // SAVE btn
    // CANCEL btn
    // Swipe to remove entry?
  );
};

export default AddTask;
